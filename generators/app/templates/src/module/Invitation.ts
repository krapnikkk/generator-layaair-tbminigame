import { UIWindow } from "../core/ui/UIWindow";
import UI_Main from "./Invitation/UI_Main";

export default class Invitation extends UI_Main {

    window: fgui.Window;
    constructor() {
        super();
    }

    initWindow(): void {
        this.window

    }

}